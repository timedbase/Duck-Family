SUBGRAPH_URL=https://api.goldsky.com/api/public/project_cmhtxnzpqm81001w94ksmgira/subgraphs/duckfun-ink/current/gn
INK_RPC_URL=https://rpc-gel.inkonchain.com

# /arc routes only -- see ../subgraph-arc/README.md for why this isn't on
# Goldsky. Both optional: missing either 502s only /arc/... routes, not the
# whole API (see chain/client.ts, subgraph/client.ts).
ARC_RPC_URL=https://rpc.arc-scan.org
ARC_SUBGRAPH_URL=http://duckfun-graph-node:8000/subgraphs/name/duckfun-arc

# API
PORT=3000

# Token-create image/metadata uploads (pinata.cloud API JWT) — server-side
# only, never sent to the browser.
PINATA_JWT=your-pinata-jwt
